{
  "name": "@unleashd/ingest",
  "version": "0.1.0",
  "private": true,
  "description": "Transcript ingestion: FSEvents watcher, incremental per-provider parsers and one SQLite store, as an in-process napi-rs addon",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "ingest"
  },
  "scripts": {
    "build": "pnpm --package=@napi-rs/cli@3.10.5 dlx napi build --release --no-const-enum --dts index.d.ts",
    "build:debug": "pnpm --package=@napi-rs/cli@3.10.5 dlx napi build --no-const-enum --dts index.d.ts",
    "test": "cargo test --manifest-path ../Cargo.toml -p unleashd-ingest --no-default-features && node ../../tools/ensure-addons.mjs unleashd-ingest && node --test test/node.test.mjs test/records.test.mjs"
  }
}
