# Lean Buddy repairs — September 16, 2026

## Accepted scope

Owner in conversation `9c15524a-dd2a-4045-9770-a82693614c62` approved reorganizing the reviewed Tasks, cutting/merging redundant work and using the standing team to finish repairs. This follows the September 15 lean review and September 16 explicit manual-review boundary. No new entities, schema fields, endpoints, MCP tools/options or expanded output contracts are authorized by this repair pass. Present any necessary expansion separately before implementation. Recall output changes are deferred.

The historical review is `../2026-09-15-lean-review/README.md`, SHA256 `d48c061557c14f98b3a4445edfb08dd8cf9b232075535ceb084b9605177e975b`. The baseline JSON preserves fresh native records and the selected dispositions: six cuts, eighteen merges, five deferrals, four delivery/repair Tasks, one coordination Task and one routing obligation. Native Tasks remain the current authority.

Two superseded parents (37f2780f and 9864f34e) must remain execution-enabled until their existing children finish: native cancellation fences descendants and there is no exposed reparent operation. Their duplicate acceptance criteria transfer to the canonical owners now; their closure follows child completion. Do not add reparenting or bypass this boundary to make the counts look tidy.

## Work boundaries

- Lead: native Task reconciliation; existing readiness return-destination selection; effective identity and run/root stop clarification using existing refs/fields; summary/filter qualification. Own server readiness/operation tests and operator docs. Do not change recall output.
- UI Engineer (`95592e35`): current shared Team settings draft preservation, existing saved-grant revocation paths, hierarchy/badge qualification, and read-only original-thread history evidence. Own client settings and only directly necessary owner-route changes, preserving current API. No server readiness or package edits.
- Release Engineer (`a2047be3`): fix omitted project/todo evidence loss in canonical package authority, preserve completion invalidation and explicit replacement semantics; native boundary regression for self and supervised edits; then integrate accepted Lead/UI changes into one attributable candidate and verify existing boundaries. Own package source, vendored archive/provenance and install coordination. Keep prior dirty source intact; distinguish inherited snapshot and repair commits.

Use existing Tasks and fresh bounded native work requests, without reviving exhausted requests. Record results against the originating request. No forced server restart, main push, deployment, paid research or external account changes. Do not add extra workers or programs. Preserve existing tests for privacy, scope, deadlines, cancellation and lifecycle. Parent review inspects actual evidence; execution success does not establish completion.

## Decision rationale

The failure drivers are evidence deletion coupled to completion invalidation, projections that choose a different return target than execution, ambiguous selection between portable identity and scoped publication, and form drafts keyed by fetched revision. Correct these authorities instead of adding parallel state. Older proposals remain historical evidence. Revisit scope only for a reproduced workflow gap that existing primitives cannot solve and with manual owner approval for any expanded surface.
