# T22-S5: Buddy UI features 5–8 restored (2026-09-26)

Worktree `.claude/worktrees/lane-buddy-ui`, branch `feat/buddy-ui-restore`, on b3cd090. Not pushed, not merged. No channel components touched.

## Commits
- fd87aff F5 Work tab: New task / New todo forms (`POST /tasks`), Move up/down (`PATCH position`), Pause/Resume (`PATCH paused`) on every task card (controls sit in the summary, and a click on them does not toggle the card) and every todo. Siblings sort by position, then creation. The crate creates top-level tasks at 0, so the first move renumbers the list and later moves write two tasks. Test: `client/test/buddy-work.test.tsx` (move math + render).
- a3e3c02 F6 Memory tab: a History disclosure on every doc (`/docs/:docId/revisions`, loaded only while open). Notes and Shared docs lists (`/docs/:kind?all=1`), each opening the same revision-checked editor at the doc's own scope (buddy, workspace, task or thread). A New doc form writes a note or a shared doc (Buddy or workspace scope) at base revision 0, so a name clash returns 409 and never overwrites. Test: `client/test/buddy-memory.test.tsx`, including a scope guard: a dropped scope would read the Buddy-scoped doc of that name.
- c053499 F7 Messages tab: each DM post shows "N replies · last reply" from the page's thread stats and opens into its thread (`/posts/:id/thread`) with a reply form. "Waiting on <Buddy>" lists `inbox.waitingOn` narrowed to the DM, oldest first. Test: added to `client/test/buddy-messages.test.tsx`.
- 9afa8c3 F8 Directory cards show "N open" and "M blocked". New crate fn `taskCounts(workspaceId)`: unfinished top-level tasks per Buddy, with the blocked ones among them. It runs on a new partial index `task_live(workspace_id, owner_id, status) WHERE parent_id IS NULL AND status IN (live)`, created on open with IF NOT EXISTS. Plan: `SEARCH task USING INDEX task_live`, and the query-plan workload now covers it. The overview route adds `taskCounts` per workspace. Tests: crate `task_counts_are_unfinished_top_level_tasks_per_buddy`, a route assertion in `buddies-v2.test.ts` (restored-features test) and `client/test/buddy-directory.test.tsx`.

## CSS
+0 lines. Everything reuses existing classes (`buddy-panel__form/actions`, `buddy-post-list`, `buddy-work-disclosure`). G8: 14975 / 14975.

## Checks (clean committed tree, `git status --porcelain` empty)
- typecheck: pass. test:client: 152/152. test:server: 253/253. invariants: all 8 gates pass. vite build: pass.
- Crate (throttled, shared target): cargo tests pass (core 25, query_plan, import) and node addon test 2/2. `cargo fmt` is clean. The addon was rebuilt and index.d.ts regenerated.

## Notes
- Guard hit: `git checkout -- <file>` was blocked by dcg. An over-broad `biome check --write` had reformatted 3 sigil files (BuddySigil.tsx, sigil/client.ts, sigil/render.ts). I did not retry the checkout. I undid biome's edits by hand, so those files match HEAD and are not in any commit.
- Mobile directory rows don't show the counts; only the desktop cards were in scope.
- "Waiting on" covers only the owner's requests in this Buddy's 1:1 DM, not group DMs.
- A new top-level task starts at position 0 (crate default), so before the first reorder it sorts among the other zero-position tasks by creation time.
- No screenshots run (no live server).
