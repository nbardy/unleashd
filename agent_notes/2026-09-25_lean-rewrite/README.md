# Lean rewrite (2026-09-25/26): plan and evidence

A snapshot of the working folder `~/git/unleashd-lean-scope/`. That folder is the live copy the orchestrator updates; this
snapshot is refreshed at each wave. The database copies and scratch tooling stay out of git.

- `DESIGN.md`: the design (principles, the Buddies model, the Rust plan, decisions).
- `PROJECT.md`: the board (tasks, dependencies, status, owner gates).
- `00`–`06`: the measurements, the inventories, and the server and client target designs with line budgets.
- `T*.md`: one report per task. `BENCH-*.md`, `THREAD-LOAD.md`: benchmarks and investigations.
- The structural patterns live in `docs/patterns.md`. The reusable method is the `deslop` skill.
