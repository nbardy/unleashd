# T22-S4: client side of restored Buddy features 1–4 and 9 (2026-09-26)

Worktree `.claude/worktrees/lane-buddy-ui`, branch `feat/buddy-ui-restore`. Merged origin/lean/integration (f6f629b) as cb328ac with no conflicts. Not pushed.

## Commits (on top of the WIP ee12f02, which already held the client code for features 1–3)
- e69a2ea F1: reply counts and last reply render test (`client/test/channel-restored.test.tsx`).
- bd1bca8 F2: "New messages" divider (the day row in red, not sticky), bold threads with a dot. Test: the line goes above the first post by someone else after `lastReadOrd`, and the owner's own posts and replies never count as new.
- 2d5b0cf F3: desktop permalink test. It also fixes `mobile-channels.test.tsx`, which ee12f02 broke: the thread now requests `&from=<post>`. Comment updated in channel-link.ts.
- 1b61e5f F4: `?task=` Task filter (desktop only). Adds `RowPlace` Task, where each row names its channel as a permalink into it. The picker lists the channel's Tasks plus the one in the URL. The `task-filter` screenshot screen is back. Screenshot discovery now reads `threads` stats instead of probing 20 threads per channel.
- c7b6b8f F9: Settings always offers Default for provider, model and effort, and a cleared field is sent as `null`. Test in `client/test/buddy-settings.test.tsx`.
- 17e8cc6 F4 fix: `channel_changed` now also refreshes `/api/buddies/tasks/<id>/posts`. Without it, a filtered view only updated on the 30 s backstop. The resource-cache test covers it, and the docs name the route.

## CSS (G8)
+29 lines, offset in the same commits:
- merged identical rules in ChannelBrowser.css
- dropped the `100vh` fallback in front of `100dvh`
- replaced 7 flex+center pairs with the `ui-row` primitive

Total 14980 → 14975, and the G8 ceiling is lowered to 14975.

## Checks (clean committed tree, `git status --porcelain` empty)
- typecheck (incl. `tsc -b`): pass
- test:client: 144/144
- test:server: 253/253 (run before 17e8cc6, which changes only the client)
- invariants: all 8 gates pass
- vite build: pass

## Notes / left
- Feature 1 is desktop only; the old mobile had no counts either. Mobile thread links are unchanged.
- In the Task filter, a row's "reply" is its channel link (the thread pane is per channel).
- Viewing the filter still marks the open channel read.
- No screenshots run (no live server used). `cargo clean` skipped, as the coordinator's shared-target message asked; crate builds ran before that message.
