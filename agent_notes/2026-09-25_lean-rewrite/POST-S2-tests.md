# POST-S2 tests (fix/post-s2, 47468ed..e1c4b73, lane-post-s2 worktree)

1. test:api root cause was NOT Buddies/records: T09 (c83724b, protocol v3) replaced init/conversation_created/command_rejected with hello/ack/rows/patch/removed; test/api.test.js still spoke v2, so every `init` wait timed out (2/16). The server already booted a fresh dir: records has `fresh` (config-records.ts) and a missing Buddies DB only logged. Ported the suite to v3 (1fa39a1). Config/queue/session now come from the detail route, and the Claude default comes from the catalog (it moved opus -> claude-opus-5-5). Result: 16/16.
2. Real first-time-user bug found on the way: with no `~/.buddies/buddies.sqlite` at all, boot said "import the v33 database first" (there was nothing to import), so Buddies never worked. Fixed in the server (3e4240b): `BuddiesLocation = database | fresh | unimported` mirrors recordsLocation. Only a present v33 file refuses; a fresh install opens an empty DB. Pattern: fix-guards. There are 2 tests in buddies-v2.test.ts. AGENTS.md and the crate README are updated (a6fb2e2).
3. test:package (e1c4b73): test/package-smoke.js (117 lines). It runs `pnpm build`, resolves buddies-core.node + ingest.node from server/, boots server/dist on a temp dir + spare port, then hits the provider catalog, `/`, and a Buddies workspace POST + overview read. It replaces `pnpm build` in the root `test` chain (one build). docs/MAINTENANCE.md is updated.
4. Checks, run on a clean tree (== HEAD): root `pnpm test` exit 0 (typecheck, build + package smoke, server 184, client 152, dev-supervisor, tools, cli 273, api 16/16). check-client-invariants 8/8 PASS; biome clean on touched files.
No push, merge, stash or reset.
