# Importer fixes before T15 (branch fix/importer-robustness, worktree lane-importers; not pushed or merged)
- d45f174 The import report now has `dropped_tables` (table, rows) for all 11 tables, and the CLI prints them. Test: `report_counts_every_dropped_table`, which did not compile before the fix.
- cf9e6b3 The soul check reads the soul on both sides as Present or Absent. Absent on both sides counts as `no_soul`. A soul missing from the target is `doc_lost`, a soul only in the target is `doc_invented`, and both fail. Test: `a_buddy_without_a_soul_verifies_but_a_lost_soul_fails`. Before the fix it failed with QueryReturnedNoRows.
- 8b69d7a record-migration.ts leaves a corrupt file untouched in by-conversation/, lists it under the report's `rejected` (not under `failures`), then continues. Test in `server/test/record-migration.test.ts`. Before the fix it failed with a JSON SyntaxError.
Dry run under db/t15-dryrun/run2-importers/ (the v33 snapshot's sha256 was unchanged afterwards):
- Buddies: import exit 0. Counts equal the first dry run (buddy 52, run 2776, event 22109, ...). The report lists 77 rows that were not imported: sprints 3, work_items 15, skills 4, delegations 10, reviews 2, approvals 3, builder_creations 9, access_grants 2, mail 0+0, checkpoints 29.
- Verify against the live soul files: exit 1, `marketing-designer: differ`. That soul was edited at 03:53, after the 02:37 snapshot, so the gate caught real drift. Against the snapshot souls (a copy with rewritten paths, in snapshot-souls/): exit 0, ok=true, souls {match 45, match_no_header 4, no_path_empty 3}.
- Records: migration 8180/8180 with 0 failures and 0 rejected. Import 8180 plus 1 reject. Verify ok=true: 8180 hash-equal, 1/1 rejects byte-equal, index 9120/9120.
- Records with a corrupt file added: migration exit 0 with 1 rejected (corrupt_json). Import 8180 of 8181 plus 2 rejects. Verify ok=true with 2/2 rejects byte-equal.
- The gate still blocks. When I deleted that reject row, records verify exited 1 (the file showed as unaccounted). When I deleted a soul doc, Buddies verify exited 1 with `doc_lost`.
Checks on the clean committed tree (8b69d7a, git status empty): `pnpm test` exit 0 (server 188 pass + 1 todo, client 159, dev-supervisor 14, tools 5, cli 273, api 16); invariants: all 8 gates pass. Crate tests: buddies 26, ingest 54, buddies-import 4, records-tool 3, all pass. A plain `cargo test --workspace` fails to link the napi addons; each addon crate has to run with `--no-default-features`, as its package.json does.
Runbook: no commands changed. T15-RUNBOOK.md now notes the branch to merge before step 0, the `dropped_tables` and `rejected` report fields, the new soul verdicts, and that soul verify reads the live soul files.
