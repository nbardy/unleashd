# POST-S2 labels + docs + rename (branch fix/usage-limit-labels, worktree lane-labels; not pushed)
- cf0dd71 merge origin/lean/integration (47468ed). Conflicts in usage-routes.ts and the parity test resolved by taking S2's ingest-backed files, then re-applying f7cb17d in `codexLimits`: every Codex window that exists gets the label `${rateWindowLabel(min)} limit`. The parity golden now expects '7d limit'. RateLimitGroup and its tests auto-merged unchanged.
- be06343 docs/architecture.md 2.0/2.1 rewritten: the provenance rule in `ingest/runtimes.ts` recover/materialize, history on demand (`list.page` → `sessionMessages` → `Ingest.messages`, the v3 messages endpoint), Codex display rules in `crates/unleashd-ingest/src/parsers/codex.rs`, and `record.kind` as the one source of Buddy identity. References to the deleted session-loader-hydration test, disk-adapter and session cache are removed.
- 5fb7133 `hydrate({ legacy })` renamed to `sessionEvidence` in config-service.ts, server.ts, ingest/runtimes.ts and config-service.test.ts. `configFromSessionEvidence` already had the right name.
- Checks on the clean committed tree (5fb7133, porcelain empty): typecheck, test:server 184/184, test:client 153/153, test:cli 273/273, invariants and vite build all passed.
- Note: the lines 88 and 118 remarks in section 2 that the session cache and adapters were deleted are historical and still correct, so I left them.
